version: "3.9"
services:
  api:
    build: ./app
    ports:
      - "8000:8000"
    environment:
      - ENV=production
  dashboard:
    build: ./dashboard
    ports:
      - "8501:8501"
    depends_on:
      - api
    environment:
      - API_URL=http://api:8000
